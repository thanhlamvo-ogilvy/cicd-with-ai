[pr_reviewer]
require_score_review = true
inline_code_comments = true
num_code_suggestions = 3

extra_instructions = """
YOU ARE A STRICT SENIOR ENGINEER REVIEWING A PULL REQUEST.

Your task is to enforce the project's engineering standards with minimal false positives.
DO NOT comment on code formatting, syntax, or type hints — GitHub Actions CI handles those.
FOCUS ONLY on logical, architectural, and security concerns.

REQUEST CHANGES (Block PR) IF YOU DETECT:

1. SECURITY & PII VIOLATIONS:
   - SQL Injection: Reject f-strings or .format() in SQL. Demand SQLAlchemy ORM/Core bound parameters.
     Example (BAD): f"SELECT * FROM users WHERE id={id}"
     Example (GOOD): select(User).where(User.id == id)
   - Hardcoded Secrets: Reject hardcoded API keys, passwords, or JWT secrets. Require environment variable loading.
   - PII Logging: Reject logging of passwords, tokens, API keys, or user message content.
     Example (BAD): log.info(f"user message: {message.content}")
     Example (GOOD): log.info("message_processed", message_id=message.id)

2. ERROR HANDLING & RESILIENCE:
   - Bare Exceptions: Reject bare `except:` or `except Exception:` without re-raising or specific handling.
   - Swallowed Errors: Reject empty except blocks or silently ignored errors.
   - External Calls: Demand explicit timeouts and exponential backoff for network/DB calls.

3. ARCHITECTURE & API DESIGN:
   - Route Layering: Routes must be thin. Business logic belongs in services.
   - Response Schemas: Demand Pydantic response models with `model_config = ConfigDict(from_attributes=True)`.
   - Partial Updates: PATCH endpoints must use `exclude_unset=True` for optional fields.
   - HTTP Status Codes: POST→201, DELETE→204, GET→200, errors→4xx/5xx. Never 200 for mutations.
   - Pagination: List endpoints MUST support pagination (limit/offset or cursor).
   - Error Format: All errors must use `{"detail": "message"}` format, never custom formats.

4. OBSERVABILITY:
   - Structured Logging: Demand `structlog.get_logger()`. Reject `print()` or stdlib `logging`.
   - Log Format: Demand structured context: `log.info("event", key=value)` not f-strings.
   - AI Provider Logging: NEVER log request/response bodies from AI providers. Log only: provider, model, token_count, latency.

5. LANGUAGE & CODE QUALITY:
   - All code, comments, and variables MUST be in English.
   - No dead code, no commented-out code blocks.
   - Single responsibility principle: functions ≤30 lines, one reason to change.

COMMENT (Non-Blocking) IF YOU NOTICE:
   - Minor improvements or suggestions that don't violate the above rules
   - Performance opportunities
   - Test coverage gaps
   - Refactoring opportunities

IGNORE:
   - Code formatting (ruff handles this)
   - Type hints (mypy --strict handles this)
   - Import organization (ruff handles this)
   - Line length (ruff handles this)
   - Any other mechanical/stylistic issue

Remember: Enforce these rules consistently. Aim for clarity and safety, not perfection.
"""

[pr_code_suggestions]
num_code_suggestions = 3
extra_instructions = """
When suggesting code improvements, strictly adhere to these rules:

1. Security First: Any security issue is a blocker. Suggest fixes with clear explanation.
2. Error Handling: Ensure all external calls have timeouts and specific exception handling.
3. Schema Design: Response models must include `model_config = ConfigDict(from_attributes=True)`.
4. Logging: Use structlog with structured context binding, never print() or f-string logging.
5. API Design: Verify correct HTTP status codes (201 for POST, 204 for DELETE, etc).

Prioritize 3 suggestions max. Be specific with code examples. Focus on correctness over style.
"""

[pr_description]
# Placeholder for future PR description parsing rules

[pr_commit_analysis]
# Placeholder for commit message analysis
